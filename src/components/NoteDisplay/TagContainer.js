import styled from 'styled-components';

export default styled.form`
    display:grid;
    grid-template-columns:auto 1fr;
    gap:1rem;
    position:absolute;
    bottom:0;
    width:100%;
    height:10vh;
    overflow:scroll;
    overflow-x:hidden;
    justify-items:start;
`;